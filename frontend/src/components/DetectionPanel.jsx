import React from 'react'
export default function DetectionPanel({ detections, selected, onSelect }) { return <aside className="detection-panel"><div className="panel-title"><span>DETECTIONS</span><b>{detections.length} OBJECTS</b></div><div className="filter-row"><button className="selected-filter">All</button><button>Debris</button><button>Anomaly</button></div>{detections.length ? detections.map(d => <button className={`detection-row ${selected?.id === d.id ? 'selected' : ''}`} onClick={() => onSelect(d)} key={d.id}><div><b>{d.id}</b><span>{d.class}</span></div><strong>{Math.round(d.confidence * 100)}%</strong><small>{d.latitude ? `${d.latitude.toFixed(4)}, ${d.longitude.toFixed(4)}` : 'No geolocation'}</small></button>) : <p className="muted">No detections loaded. Run analysis when an AI service is configured.</p>}</aside> }
